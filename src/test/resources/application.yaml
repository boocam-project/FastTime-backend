spring:
  profiles:
    include: test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydatabase?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: user
    password: password
  mail:
    host: "smtp.naver.com"
    username: test
    password: 1234

  jwt:
    secret: THISISTHEJWTSECRETKEYFORTESTAPPLICATIONDONTUSETHISCODEINPRODUCTIONPROFILE

  data:
    redis:
      host: localhost
      port: 6379

cors:
  allowed-origins:
    - http://localhost:3000