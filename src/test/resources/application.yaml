spring:
  profiles:
    include: test
  datasource:
    driver-class-name: org.h2.Driver

  flyway:
    enabled: false

  mail:
    host: "smtp.naver.com"
    username: test
    password: 1234

  jwt:
    secret: THISISTHEJWTSECRETKEYFORTESTAPPLICATIONDONTUSETHISCODEINPRODUCTIONPROFILE

  data:
    redis:
      host: localhost
      port: 6379

cors:
  allowed-origins:
    - http://localhost:3000

discord:
  hook-name: spring-hook-dev
  url: test